<h1><%= @room.name %></h1>
<div id='messages' data-room_id="<%= @room.id %>">
  <%= render @messages %>
</div>

<%= text_area :content, nil, data: { behavior: 'room_speaker' }, placeholder: "コメント内容", maxlength: 200 %>